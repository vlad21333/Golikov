<Window x:Class="Golikov.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Golikov"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputBox" Grid.Row="0" FontSize="24" HorizontalAlignment="Stretch" Margin="10" IsReadOnly="True"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Content="7" Grid.Row="0" Grid.Column="0" Click="Number_Click"/>
            <Button Content="8" Grid.Row="0" Grid.Column="1" Click="Number_Click"/>
            <Button Content="9" Grid.Row="0" Grid.Column="2" Click="Number_Click"/>
            <Button Content="/" Grid.Row="0" Grid.Column="3" Click="Operation_Click"/>

            <!-- Аналогично остальные кнопки: 4,5,6,*,1,2,3,-,0,.,=,+,^,C -->
        </Grid>
    </Grid>
</Window>
